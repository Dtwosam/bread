# Bread handoffs

`docs/current-build-state.yaml` is the machine-readable current state.

Optional immutable handoff snapshots may be stored here as `<checkpoint>-<commit>.md` after accepted checkpoint closures.

Never store private keys, seed phrases, API tokens, recovery codes, or secret values in handoff files.
